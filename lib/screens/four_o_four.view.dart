import 'package:flutter/material.dart';

class FouroFourView extends StatelessWidget {
  const FouroFourView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Something went wrong"),
      ),
    );
  }
}
